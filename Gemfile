source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in bot_delive.gemspec
gemspec
gem 'rest-client'
gem 'activesupport'
